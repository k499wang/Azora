/**
 * The real app icon, used wherever the UI mocks up a notification from Azora so
 * the preview looks like the thing it is promising.
 */
export const APP_ICON_ASSET = require('../../assets/app/icon.png');

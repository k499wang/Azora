/**
 * The celebration flame, AI-generated and kept as a flat PNG rather than paths:
 * it carries a gradient and a contact shadow that inline SVG would either
 * flatten or make expensive to draw at 260pt.
 */
export const STREAK_FLAME_ASSET = require('../../assets/app/streak-flame-ai.png');

/** Shared pacing for onboarding screen entrances and their first content. */
export const entranceTiming = {
  fade: 1150,
  scale: 1220,
  copy: 900,
  subtitleDelay: 240,
  prompt: 700,
  promptDelay: 180,
  asideDelay: 320,
  option: 720,
  optionDelay: 290,
  optionStagger: 65,
  visual: 760,
  visualDelay: 180,
  cardStagger: 220,
  cardDelay: 340,
} as const;
